name: Simple workflow

on: 
  schedule:
    - "cron: */5 * * * *"
  
jobs: 
  deploy:
    environment: prod
    name: Simple workflow
    runs-on: ubuntu-latest

    steps: 
      - name: list items
        run: ls -a